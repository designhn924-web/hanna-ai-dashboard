# components

複数の機能から共通で使う、再利用可能なUIコンポーネントを置きます。
特定の機能に依存しない汎用的なパーツ(ボタン、カード、モーダルなど)がここに入ります。

特定の機能専用のコンポーネントは `features/` の該当機能フォルダに置いてください。

ダッシュボード全体の骨格(ヘッダー・サイドバー・共通レイアウト)は `components/layout/` にまとめています。
